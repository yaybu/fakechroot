language: python
python:
  - "2.6"
  - "2.7"
  - "3.3"
  - "3.4"
  - "pypy"

before_install:
  - sudo apt-get update
  - sudo apt-get install -y -q debootstrap fakeroot fakechroot cowdancer

install:
  - pip install -r requirements.txt

script: nose2
